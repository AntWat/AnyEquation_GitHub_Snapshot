<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="AnyEquation.Common.BusyOverlay"
                 AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
                 AbsoluteLayout.LayoutFlags="All"
                 IsVisible="{DynamicResource ShowAppBusy}"
                 BackgroundColor="{StaticResource busyOverlayColor}"
                 Padding="10, 0">
  <ContentView.Content>
    <ActivityIndicator IsRunning="{DynamicResource ShowAppBusy}"
        VerticalOptions="Center" HorizontalOptions="Center" />
  </ContentView.Content>
</ContentView>
